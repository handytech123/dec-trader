# API control-plane boundary

Reserved for authenticated control-plane APIs. Phase 0 contains no server and no execution path.
Future mutations require actor identity, correlation ID, idempotency key, schema version, timestamp,
and append-only audit evidence.
